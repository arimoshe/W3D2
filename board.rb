class Board 

    def initialize
        @board= Array.new(4) {Array.new(4)}
    end

    def populate
        row = rand(0...@board.length)
        col = rand(0...@board.length)
        

    def 
end